{
  "name": "fair-playwright-test-project",
  "version": "1.0.0",
  "private": true,
  "description": "Test project for fair-playwright reporter",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
